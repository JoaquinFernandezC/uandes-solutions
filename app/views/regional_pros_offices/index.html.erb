<div class="card">
  <div class="card-header bg-info">
    <div class="row">
      <div class="col-sm-11">
        <h1 class="text-white">Fiscales Regionales</h1>
      </div>
      <div class="col-sm-1">
        <%= link_to (fa_icon 'plus lg', style: 'color: #17a2b8'), new_regional_pros_office_path, class: 'btn btn-light btn-lg float-right', style: 'margin: auto; display: block' %>
      </div>
    </div>
  </div>
  <div class="card-body">
    <div class="row" style="background: #FFFFFF">
      <div class="col-sm-12 hidden-xs">
        <div class="container">
          <div class="tab-content">
            <div id="home" class="tab-pane active">
              <div class="table-responsive">
                <table class="table table-hover">
                  <thead>
                  <tr>
                    <th>Region</th>
                    <th>Fiscal</th>
                    <th>Acciones</th>
                  </tr>
                  </thead>

                  <tbody>
                  <% @regional_pros_offices.each do |regional_pros_office| %>
                    <tr>
                      <td style="text-align: center;"><%= regional_pros_office.region %></td>
                      <td style="text-align: center;"><%= regional_pros_office.prosecutor.name %></td>
                      <td style="text-align: center;"><%= link_to 'Edit', edit_regional_pros_office_path(regional_pros_office) %> <%= link_to 'Destroy', regional_pros_office, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                    </tr>
                  <% end %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
