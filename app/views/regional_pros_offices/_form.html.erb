<%= form_with(model: regional_pros_office, local: true) do |form| %>
  <% if regional_pros_office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(regional_pros_office.errors.count, "error") %> prohibited this regional_pros_office from being saved:</h2>

      <ul>
      <% regional_pros_office.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :region, 'Región' %><br />
    <%= form.text_field :region, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :prosecutor_id, 'Fiscal' %><br />
    <%= form.select(:prosecutor_id, Prosecutor.all.collect { |prosecutor| [prosecutor.name, prosecutor.id] }, { prompt: '' }, { class: 'form-control' }) %>
  </div>

  <div class="actions">
    <%= form.submit 'Completar', class: 'btn btn-primary' %>
    <%= link_to 'Cancelar', entity_selection_index_path, class: 'btn btn-danger' %>
  </div>
<% end %>
